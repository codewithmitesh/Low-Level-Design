package SolidCheezy.DIP.ProblematicCode;

class SQLDatabase {
    // this implements the database operations
    // now if this operation changes like now we are using SQLDatabase - lets say
    // MySQL
    // tomorrow we need to change it to PostgreSQL or Oracle or DynamoDb then we
    // need to update
    // this code
    public void saveUser(String user) {
        // Code to save user to the database
    }
}
